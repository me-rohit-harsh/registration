.nav-pills-custom .nav-link {
    color: #aaa;
    background: #fff;
    position: relative;
}

.nav-pills-custom .nav-link.active {
    color: #45b649;
    background: #fff;
}


/* Add indicator arrow for the active tab */
@media (min-width: 992px) {
    .nav-pills-custom .nav-link::before {
        content: '';
        display: block;
        border-top: 8px solid transparent;
        border-left: 10px solid #fff;
        border-bottom: 8px solid transparent;
        position: absolute;
        top: 50%;
        right: -10px;
        transform: translateY(-50%);
        opacity: 0;
    }
}

.nav-pills-custom .nav-link.active::before {
    opacity: 1;
}
.nav-pills-custom .nav-link {
    color: #555; /* Default text color */
    background: transparent; /* Transparent background */
    position: relative;
    border: none; /* Remove default border */
    padding: 10px 15px; /* Add padding for better spacing */
    transition: background-color 0.2s ease-in-out, color 0.2s ease-in-out; /* Smooth hover effect */
    text-align: left; /* Align text to the left */
}

.nav-pills-custom .nav-link.active,
.nav-pills-custom .nav-link:focus,
.nav-pills-custom .nav-link:hover {
    color: #fff; /* Text color when active or hovered */
    background: #45b649; /* Background color when active or hovered */
    border-radius: 0; /* Remove border radius when active or hovered */
}

/* Add indicator arrow for the active tab */
@media (min-width: 992px) {
    .nav-pills-custom .nav-link::before {
        content: '';
        display: block;
        border-top: 8px solid transparent;
        border-left: 10px solid #fff;
        border-bottom: 8px solid transparent;
        position: absolute;
        top: 50%;
        right: -15px; /* Adjust spacing */
        transform: translateY(-50%);
        opacity: 0;
    }
}

.nav-pills-custom .nav-link.active::before {
    opacity: 1;
}

