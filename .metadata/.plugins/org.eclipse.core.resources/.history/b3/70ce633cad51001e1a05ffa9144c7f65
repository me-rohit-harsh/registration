<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
<meta charset="ISO-8859-1">
<title>Indian Banking Dashboard</title>
<link rel="stylesheet" type="text/css"
	href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css">
<link rel="stylesheet" type="text/css" href="@{/css/style.css}">
</head>
<body>
	<div class="container">
		<div class="row">
			<h1>
				Welcome to Indian Banking <span th:text="${user.fname}"></span>
			</h1>

			<table class="table table-striped table-bordered">
    <thead class="table-dark text-center">
        <tr>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Email</th>
            <th>Sex</th>
            <th>Type</th>
            <th>Date of Joining</th>
            <th th:if="${user.lDA != null}">Last Logged in</th>
        </tr>
    </thead>

    <tbody>
        <tr>
            <td th:text="${user.fname}"></td>
            <td th:text="${user.lname}"></td>
            <td th:text="${user.email}"></td>
            <td th:text="${user.sex}"></td>
            <td th:text="${user.acType}"></td>
            <td th:text="${user.dOJ}"></td>
            <td th:if="${user.lDA != null}" th:text="${user.lDA}"></td>
        </tr>
    </tbody>
</table>
			<a th:href="@{/logout}">Logout</a>
		</div>
	</div>
</body>
</html>
